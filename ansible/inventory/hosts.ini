# ── Option B : exécution depuis la VM elle-même (recommandé pour débuter)
[devops_vm]
localhost ansible_connection=local

# ── Option A : exécution depuis WSL (décommente et commente la section au-dessus)
# [devops_vm]
# ubuntu-vm ansible_host=192.168.X.X
